{{ form_start(form,{ 'attr': {'class': 'mx-1 mx-md-4'} }) }}

    <div class="d-flex flex-row align-items-center mb-4">

        <div class="form-outline flex-fill mb-0">
            <label class="form-label" for="form_pname">{{ form_label(form.pname) }}</label>
            {{ form_widget(form.pname,
            { 'attr':{'class':'colortext'} }) }}
        </div>
    </div>

    <div class="d-flex flex-row align-items-center mb-4">

        <div class="form-outline flex-fill mb-0">
            <label class="form-label" for="form_pdesc">{{ form_label(form.pdesc) }}</label>
            {{ form_widget(form.pdesc,
            { 'attr':{'class':'colortext'} }) }}
        </div>
    </div>

    <div class="d-flex flex-row align-items-center mb-4">

        <div class="form-outline flex-fill mb-0">
            <label class="form-label" for="form_price">{{ form_label(form.price) }}</label>
            {{ form_widget(form.price,
            { 'attr':{'class':'colortext'} }) }}
        </div>
    </div>
    
    <div class="d-flex flex-row align-items-center mb-4">

        <div class="form-outline flex-fill mb-0">
            <label class="form-label" for="form_ifile">{{ form_label(form.ifile) }}</label>
            {{ form_widget(form.ifile,
            { 'attr':{'class':'colortext'} }) }}
        </div>
    </div>

    <div class="d-flex flex-row align-items-center mb-4">

        <div class="form-outline flex-fill mb-0">
            <label class="form-label" for="form_cat">{{ form_label(form.cat) }}</label>
            {{ form_widget(form.cat,
            { 'attr':{'class':'colortext'} }) }}
        </div>
    </div>

    <div class="d-flex justify-content-center mx-4 mb-3 mb-lg-4">
    {{ form_widget(form.save) }}
    <a class="btn btn-primary ms-2" role="button" href="{{ path('app_product_index') }}">back to list</a>
</div>

{{ form_end(form) }}
