{% extends 'base.html.twig' %}

{% block title %}Branch{% endblock %}

{% block body %}
    <h1>Branch</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ branch.id }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ branch.Address }}</td>
            </tr>
            <tr>
                <th>Bname</th>
                <td>{{ branch.bname }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_branch_index') }}">back to list</a>

    <a href="{{ path('app_branch_edit', {'id': branch.id}) }}">edit</a>

    {{ include('branch/_delete_form.html.twig') }}
{% endblock %}
